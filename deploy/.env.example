# deploy/.env.example — Production environment for GCP VM + Cloud SQL
#
# Copy this file:
#   cp deploy/.env.example deploy/.env
# Then edit deploy/.env with real values.

# ── Cloud SQL Auth Proxy ──
CLOUDSQL_INSTANCE_CONNECTION_NAME=blueth-city:us-central1:blueth-postgresql

# ── Database ──
DB_USER=blueth
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=blueth_city

# ── API Server ──
PORT=3001
LOG_LEVEL=info

# ── CORS ──
# Comma-separated origins. Supports wildcards: *.itch.io
# Example: https://yourgame.itch.io,*.itch.io,*.hwcdn.net
ALLOWED_ORIGINS=*.itch.io,*.hwcdn.net

# ── HTTPS Domain ──
# Option 1: Real domain (requires DNS A record → VM external IP)
#   API_DOMAIN=api.yourdomain.com
#
# Option 2: Quick start with sslip.io (no DNS needed)
#   API_DOMAIN=api.34.42.121.95.sslip.io
API_DOMAIN=api.34.42.121.95.sslip.io

# ── Workers ──
SCHEDULER_POLL_MS=5000
TICK_POLL_MS=10000
